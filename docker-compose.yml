services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: rolemark
      POSTGRES_PASSWORD: rolemark
      POSTGRES_DB: rolemark
    ports:
      - "5432:5432"
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
